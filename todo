1) edit transactions.py to support the right transactions
2) have it automatically sign blocks it is able to, and have it automatically reveal secrets, and automatically pledge
3) if we are talking to a peer, and they tell us a contradiction, then we should create the slasher transaction to punish
4)sig_length should count more for people who have more than 1/64th of the money.