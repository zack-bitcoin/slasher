import api, custom, tools, time

def create_reward_tx():
    pass

def mainloop():
    while True:
        time.sleep(0.5)
        #if I can make the right kind:
            #api.easy_add_transaction(create_reward_tx())
        #else:
            #time.sleep(1)
        
